public class TransferCommandTest {

	/*
	 * @Test public void test_valid_transfer_between_checking_accounts() { Bank bank
	 * = new Bank(); Checking checking = new Checking("12345678", 0.03);
	 * Bank.addAccount(checking); Checking checking2 = new Checking("87654321",
	 * 0.02); Bank.addAccount(checking); Bank.addAccount(checking2);
	 * 
	 * checking.deposit(500); checking2.deposit(300);
	 * 
	 * TransferCommand transfer = new
	 * TransferCommand("transfer 12345678 87654321 200"); boolean isValid =
	 * transfer.validate(bank);
	 * 
	 * assertTrue(isValid); transfer.execute(bank);
	 * 
	 * assertEquals(300, Bank.getAccountByID("12345678").getBalance());
	 * assertEquals(500, Bank.getAccountByID("87654321").getBalance()); }
	 */
}
